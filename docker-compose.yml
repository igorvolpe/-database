version: '3.8'
services:
  db:
    image: postgres:16
    container_name: eldorado_project_db
    environment:
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: iot_db
    ports:
      - "5432:5432"
    volumes:
      - ./database:/docker-entrypoint-initdb.d
